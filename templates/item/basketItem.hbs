<form class="sr5-marketplace sheet item-sheet" autocomplete="off">
    <header>
        <h1><input type="text" name="name" value="{{name}}" /></h1>
        <div class="flexrow">
            <label>{{localize "SR5.Marketplace.Basket.Description"}}</label>
            <textarea name="system.description.long">{{system.description.long}}</textarea>
        </div>
    </header>

    <section class="contents">
        <h2>{{localize "SR5.Marketplace.Basket.Contents"}}</h2>

        <!-- Item Add Section -->
        <div class="add-item-section">
            <div class="flexrow">
                <label for="item-select">{{localize "SR5.Marketplace.Basket.SelectItem"}}</label>
                <select name="item-select">
                    {{#each game.items.contents}}
                    <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="flexrow">
                <label for="item-quantity">{{localize "SR5.Marketplace.Basket.Quantity"}}</label>
                <input type="number" name="system.marketbasket.basketQuantity" value="{{system.marketbasket.basketQuantity}}" min="1" />
            </div>
            <div class="flexrow">
                <label for="item-rating">{{localize "SR5.Marketplace.Basket.Rating"}}</label>
                <input type="number" name="system.marketbasket.basketPrice" value="{{system.marketbasket.basketPrice}}" min="0" />
            </div>
            <button type="button" class="add-item">{{localize "SR5.Marketplace.Basket.AddItem"}}</button>
        </div>

        <!-- Basket Contents -->
        <div class="basket-contents">
            <h3>{{localize "SR5.Marketplace.Basket.CurrentContents"}}</h3>
            <ul>
                {{#each system.marketbasket.basketItems}}
                <li>
                    <span>{{name}} (x{{basketItemQuantity}})</span>
                    {{#if basketItemRating}}
                    <span> - {{localize "SR5.Marketplace.Basket.Rating"}}: {{basketItemRating}}</span>
                    {{/if}}
                    <button type="button" class="remove-item" data-index="{{@index}}">
                        {{localize "SR5.Marketplace.Basket.Remove"}}
                    </button>
                </li>
                {{/each}}
            </ul>
        </div>
    </section>
</form>
